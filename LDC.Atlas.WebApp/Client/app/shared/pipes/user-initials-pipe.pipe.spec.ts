import { UserInitialsPipePipe } from './user-initials-pipe.pipe';

describe('UserInitialsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UserInitialsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
