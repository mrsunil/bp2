import { NumberDecimalsDirective } from './number-decimals.directive';

describe('NumberDecimalsDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberDecimalsDirective(null);
    expect(directive).toBeTruthy();
  });
});
