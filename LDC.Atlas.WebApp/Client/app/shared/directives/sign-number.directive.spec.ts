import { SignNumberDirective } from './sign-number.directive';

describe('SignNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new SignNumberDirective(null);
    expect(directive).toBeTruthy();
  });
});
