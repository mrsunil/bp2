export class BankAccount {
    bankId: string;
    description: string;

    constructor(bankId: string, description: string) {
        this.bankId = bankId;
        this.description = description;
    }
}
